<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link rel="stylesheet" href="./css/bootstrap.min.css">
</head>
<body style="text-align: center;">
    <h1>Update product</h1>
    <button class="btn">
        <a href="/logout" style="color: red;">Logout</a>
    </button>
      <form style="margin-top: 50px;" action="/upload/{{product._id}}" method="post" enctype="multipart/form-data">
          <label>Image</label>
          Upload <input type="file" name="fileUpload"/>
          <input type="submit" value="upload">
     </form>
     <div>
          <img style="max-width:200px; max-height:200px" src="{{product.fileName}}">
     </div>
    <form action="/update" method="post">
        <input style="margin: 30px;" type="hidden" name="txtId" value="{{product._id}}">
        <input style="margin: 30px;" type="hidden" name="txtFileName" value="{{product.fileName}}">
        <div style="margin: 30px;">
            <b>Code</b> <input type="text" name="txtCode" value="{{product.code}}">
        </div>
        <div style="margin: 30px;">
            <b>Name</b> <input type="text" name="txtName" value="{{product.name}}"/>
        </div>
        <div style="margin: 30px;">
             <b>Price</b> (VND) <input type="text" name="txtPrice" value="{{product.price}}"/>
        </div>
        <b style="margin: 10px;">Description</b>
        <div style="margin: 10px;">
            <textarea style="margin: 10px;" name="txtDesc">{{product.description}}</textarea>
        </div>
        <div>
            <input type="submit" value="Edit">
            <div style="margin: 10px">
                <button class="btn btn-secondary"><a href="../#">Cancel</a></button>
            </div>
        </div>
    </form>
</body>
</html>
